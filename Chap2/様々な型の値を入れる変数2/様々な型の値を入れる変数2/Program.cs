using System;

class Program
{
    static void Main(string[] args)
    {
        var alpha = 123;
        var beta = "abc";
        var gamma = DateTime.Now;
        Console.WriteLine(alpha);
        Console.WriteLine(beta);
        Console.WriteLine(gamma);
    }
}
